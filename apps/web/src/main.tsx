import { createRoot } from 'react-dom/client';
import './style.css';

createRoot(document.getElementById('root')!).render(
  <main>
    <p>開発プレビュー</p>
    <h1>CodebaseCanvas</h1>
    <p>コードの構造を、理解できる地図へ。</p>
    <p>解析とグラフ表示は準備中です。現在は起動画面のみ利用できます。</p>
    <p>リポジトリの読み込みや送信は行いません。</p>
  </main>,
);
